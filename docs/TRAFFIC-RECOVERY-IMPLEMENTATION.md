# Traffic Recovery Implementation

This batch focuses on discovery and crawl efficiency before new content expansion.

- Include indexable AI-tool pages in the sitemap.
- Exclude explicit `noindex` pages from the sitemap.
- Keep historical URLs intact during the first recovery stage.
- Freeze scheduled news generation while Google re-evaluates the site.
- Expand internal linking from hubs before adding new pages.
