# Publishing scripts (itvedas-brain/content-writer.py and news-agent.py)
# use only the Python standard library (urllib, json, pathlib, etc.).
#
# This file exists so the deployment is reproducible via a virtualenv
# (see DEPLOYMENT.md). Add pinned third-party packages here the moment
# any script grows a real dependency, then re-run:
#   pip install -r requirements.txt

