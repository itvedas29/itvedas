# Advanced IT Analytics — no sample page drafted

Same situation as Low-Code App Development, which you already said to skip: this category has **no banner
image assets** anywhere in the WorkDrive library. `Analytics Plus` and `SaaS Manger Plus` (the two products
under this category) both came back empty in the Banners folder audit. The only asset that exists for this
category at all is the generic `7.Advanced IT analytics.pptx` overview deck in `product-onepagers/` — the
same kind of category-level pitch deck every other category also has, not a product-specific banner or image.

There's also no existing ITVedas article that naturally fits a BI/analytics-platform angle the way the SIEM,
PowerShell, or Azure articles do for the other categories.

**Recommendation:** treat this the same as Low-Code — deprioritize until either (a) real banner assets show
up in the affiliate library, or (b) there's a concrete new-article angle worth writing from scratch (e.g. an
"IT analytics/BI tools for ITSM data" piece), at which point a sample page can be drafted the same way as the
others above.

No draft page was created for this category to avoid padding out the deliverable with content that doesn't
reflect what's actually available.
